<view class="section">
 
  <picker bindchange="bindPickerChange" value="{{index}}" range="{{array}}">
    <view class="picker">
      {{array[index]}}
    </view>
  </picker>
</view>


<view class="section">
  
  <picker mode="region" bindchange="bindRegionChange" value="{{region}}" custom-item="{{customItem}}">
    <view class="picker">
      {{region[0]}}-{{region[1]}}-{{region[2]}}
    </view>
  </picker>
</view>

<view class="section">
 
  <picker bindchange="bindPickerChanges" value="{{indexs}}" range="{{qu}}">
    <view class="picker">
      {{qu[indexs]}}
    </view>
  </picker>
</view>

<view class="zhifu">
    <text>需支付￥100000.00</text>
</view>
<view class="tishi">
    <text>城主只能有一个</text>
</view>


<view class="bottom">
    去支付
</view>